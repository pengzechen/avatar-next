[package]
name = "kernel_support"
version = "0.1.0"
edition = "2021"
description = "Avatar OS kernel Rust support layer (allocator, logger, panic handler)"

[dependencies]
log = { workspace = true }

[features]
default = []
